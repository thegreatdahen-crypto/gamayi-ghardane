**script.js (JavaScript)**

```javascript
function chooseMode(mode) {
    if (mode === 'normal') {
        alert('نۆڕماڵ مۆد هەڵبژێردرا!');
    } else if (mode === 'hard') {
        alert('قورس مۆد هەڵبژێردرا!');
    }
}
```
